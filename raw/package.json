{
  "name": "gmx-raw",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "create-local": "graph create perpetuals-raw --node http://localhost:8020",
    "deploy-local": "graph deploy perpetuals-raw --ipfs http://localhost:5001 --node http://localhost:8020"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.1",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}